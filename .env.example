# Optional overrides for Docker Compose.
# The stack runs without a .env file by using defaults from docker-compose.yml.
# Copy this file to .env only if you want custom local values.
# Example (PowerShell): Copy-Item .env.example .env

# PostgreSQL credentials (local development only)
# For production: use Docker secrets or external secret management.
POSTGRES_USER=library
POSTGRES_PASSWORD=library
POSTGRES_DB=library_db

# Database connection URL for the app container (host "db" in Docker network).
# Format: postgresql+psycopg://user:password@host:port/database
# If you change POSTGRES_* values, update DATABASE_URL accordingly.
DATABASE_URL=postgresql+psycopg://library:library@db:5432/library_db

# For running the app outside Docker, use localhost instead of db:
# DATABASE_URL=postgresql+psycopg://library:library@localhost:5432/library_db
